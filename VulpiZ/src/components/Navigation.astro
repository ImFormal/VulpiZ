---
const currentUrl = Astro.url.pathname;
---

<div class="nav-links">
  <a href="/quetes" class:list={['nav-link', { 'active': currentUrl === '/quetes' }]}>Quêtes</a>
  <a href="/classements" class:list={['nav-link', { 'active': currentUrl === '/classements' }]}>Classement</a>
  <a href="/boutique" class:list={['nav-link', { 'active': currentUrl === '/boutique' }]}>Boutique</a>
  <a href="/faq" class:list={['nav-link', { 'active': currentUrl === '/faq' }]}>FAQ</a>
</div>

<style>
  .nav-links {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 20px;
  }

  .nav-link {
    text-decoration: none;
    color: white;
    font-size: 1.2rem;
    text-transform: uppercase;
    padding: 1rem 2rem;
    background-color: var(--orange);
    border-radius: 15px;
    min-width: 150px;
    display: flex;
    justify-content: center;
    align-items: center;
    filter: drop-shadow(4px 4px 4px rgba(0, 0, 0, 0.25));
    box-shadow: inset 0 0 4px rgba(0, 0, 0, 1);
  }

  .nav-link.active {
    background-color: var(--ft-second); 
  }
</style>