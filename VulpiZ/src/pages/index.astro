---
import Layout from '../layouts/Layout.astro';
import TableauContent from '../components/TableauContent.astro';
import GameMode from '../components/GameMode.astro';

const pageTitle = "Solo | VulpiZ";
---

<Layout pageTitle={pageTitle}>
    <div class="content">
        <TableauContent type="patch_notes" mode="Solo" />
        <GameMode mode="Solo" />
        <TableauContent type="quetes" mode="Solo" />
    </div>
</Layout>

<style>
    .content {
        display: grid;
        grid-template-columns: 1fr 2fr 1fr;
        gap: 2rem;
        padding: 2rem;
    }
</style>